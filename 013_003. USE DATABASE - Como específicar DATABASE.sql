-- USE DATABASE
-- Basicamente quando usamos o USE mudamos o banco de dados que está no Menu acima, portanto, quando temos dois bancos de dados distintos e dermos um SELECT os dois bancos de dados aparecerão, porém, o último que constará no nosso MENU

USE ContosoRetailDW
SELECT * FROM DimProduct
