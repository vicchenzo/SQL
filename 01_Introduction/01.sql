-- PRIMEIRO COMANDO PARA SELECIONAR TODAS AS COLUNAS DE UMA TABELA
SELECT * FROM DimCustomer

SELECT * FROM DimStore

SELECT 
	StoreKey, 
	StoreName, 
	StorePhone 
FROM 
	DimStore

SELECT * FROM DimProduct

SELECT 
	ProductName, 
	BrandName 
FROM 
	DimProduct

